# Cloud Security Misconfiguration & Activity Analyzer

A Python tool that ingests raw AWS-format logs and resource configs, parses and normalizes them, detects security misconfigurations and suspicious activity, and outputs a structured report with a local dashboard.

---

## Project Structure

```
cloud-security-project/
│
├── data/
│   ├── raw_cloudtrail.json       # Raw CloudTrail-format activity logs (input)
│   ├── raw_resources.json        # Raw EC2 / S3 / IAM resource configs (input)
│   ├── parsed_access_logs.csv    # Auto-generated by parser.py
│   └── parsed_resources.csv      # Auto-generated by parser.py
│
├── analysis/
│   ├── parser.py                 # Cleans and normalizes raw JSON → CSV
│   ├── detect.py                 # Detection rules — returns findings as a list
│   ├── report.py                 # Formats findings into console output + JSON file
│   └── run.py             # Entry point — runs the full pipeline
│
├── output/
│   └── report.json               # Auto-generated on each run
│
├── app.py                        # Streamlit dashboard — reads from output/report.json
└── README.md
```

---

## How It Works

```
raw_cloudtrail.json  ──┐
                       ├──▶  parser.py  ──▶  detect.py  ──▶  report.py  ──▶  console + report.json
raw_resources.json   ──┘                                                            │
                                                                                    ▼
                                                                               app.py (dashboard)
```

Each component has one responsibility:

- **`parser.py`** reads raw, messy AWS JSON and normalizes it into clean CSVs. It handles null fields, nested structures, missing keys, and maps AWS-specific values (e.g. full policy ARNs) to simple labels.
- **`detect.py`** is pure logic. It reads the normalized CSVs, runs every rule, and returns a list of findings. No printing, no file writing.
- **`report.py`** is pure presentation. It takes the findings list and produces a formatted console report and a `report.json` file.
- **`app.py`** is the dashboard. It reads `output/report.json` and renders findings visually. It never calls `detect.py` directly.

This layered design means the detection engine is completely decoupled from the data source. Swapping in a different input format only ever touches `parser.py`.

---

## Setup

```bash
pip install pandas streamlit plotly
```

---

## Running the Analyzer

**Step 1 — Run the full pipeline.** From inside the `analysis/` directory:

```bash
python run_phase2.py
```

This will:
1. Parse `raw_cloudtrail.json` and `raw_resources.json` into normalized CSVs
2. Run all detection rules against the parsed data
3. Print a formatted report to the console
4. Save findings to `output/report.json`

**Step 2 — Launch the dashboard.** From the project root:

```bash
streamlit run app.py
```

Opens at `http://localhost:8501`. Re-run `run_phase2.py` and refresh the browser to pick up any changes.

---

## Input Data Format

### `raw_cloudtrail.json`

Follows the real AWS CloudTrail `Records` array format. Each record contains nested fields including `userIdentity`, `responseElements`, `requestParameters`, and noise fields like `eventVersion`, `awsRegion`, and `recipientAccountId`.

**Mess the parser handles:**
- `null` values on `additionalEventData`, `userAgent`, `responseElements`, and `errorMessage`
- Login status split across two fields: `responseElements.ConsoleLogin` (for console sign-ins) and `errorCode` (for API calls)
- Nested resource IDs buried inside `requestParameters` under different key names per service (`bucketName`, `instanceId`, `roleName`, etc.)
- Service-to-service calls with no human `userName` — silently skipped

### `raw_resources.json`

Follows the structure of AWS `Describe*` API responses for S3, EC2, and IAM. Contains three top-level arrays: `S3Buckets`, `EC2Instances`, and `IAMRoles`.

**Mess the parser handles:**
- `null` on `BlockDeviceMappings`, `Tags`, `IamInstanceProfile`, `ServerSideEncryptionConfiguration`
- Public access derived from `PublicAccessBlockConfiguration` (four separate boolean flags) for S3, and from presence of `PublicIpAddress` for EC2
- IAM role classification mapped from full AWS policy ARNs (e.g. `arn:aws:iam::aws:policy/AdministratorAccess`) to simple `admin` / `read-only` / `user` labels
- Open port extracted from nested `SecurityGroups → IpPermissions → FromPort`

---

## Detection Rules

The engine applies 7 rules across two normalized datasets.

### Resource Rules

| # | Rule | Severity | Condition |
|---|------|----------|-----------|
| 1 | Public Storage | HIGH | `resource_type = S3` AND `public_access = true` |
| 2 | Open SSH Port | HIGH | `resource_type = EC2` AND `port_open = 22` AND `public_access = true` |
| 3 | Over-Privileged Role | MEDIUM | `iam_role = 'admin'` |
| 4 | No Encryption | MEDIUM | `encryption = false` |

### Log Rules

| # | Rule | Severity | Condition |
|---|------|----------|-----------|
| 5 | Brute Force Attempt | HIGH | ≥ 5 failed logins from same user within 5 minutes |
| 6 | Suspicious IP Behaviour | HIGH | Same user logs in from ≥ 2 distinct IPs within 30 minutes |
| 7 | Admin Overuse | MEDIUM | Admin account performs ≥ 5 actions in a single day |

Thresholds for rules 5, 6, and 7 are defined as constants at the top of `detect.py` and can be adjusted without touching rule logic.

---

## Rule Explanations

### Rule 1 — Public Storage
**Why it's dangerous:** Anyone on the internet can read, download, or overwrite the bucket's contents — including sensitive files, credentials, or backups.

**Real-world fix:** Set bucket ACL to private, disable the "Block Public Access" override, and apply a bucket policy that restricts access to specific IAM principals.

---

### Rule 2 — Open SSH Port
**Why it's dangerous:** Exposing port 22 to `0.0.0.0/0` lets anyone on the internet attempt to authenticate. Automated bots continuously scan for open SSH ports and launch credential attacks within minutes of a new instance going live.

**Real-world fix:** Restrict SSH access to known IP ranges in the Security Group. Use AWS Systems Manager Session Manager for shell access instead of opening port 22 at all.

---

### Rule 3 — Over-Privileged Role
**Why it's dangerous:** An admin role can create users, delete resources, exfiltrate data, and modify billing — all from a single compromised key or credential.

**Real-world fix:** Apply least-privilege IAM policies. Grant only the specific actions a resource actually needs (e.g. `s3:GetObject`). Use IAM Access Analyzer to audit existing permissions.

---

### Rule 4 — No Encryption
**Why it's dangerous:** If the underlying storage volume or bucket is accessed without going through the application, the data is readable in plaintext.

**Real-world fix:** Enable S3 server-side encryption (SSE-S3 or SSE-KMS). For EC2, enable EBS volume encryption at instance creation. Enforce encryption via IAM policy conditions.

---

### Rule 5 — Brute Force Attempt
**Why it's dangerous:** Repeated login failures indicate an automated attempt to guess credentials. A successful guess gives an attacker full access to that user's permissions.

**Real-world fix:** Enable MFA on all IAM users. Set an account lockout policy. Use CloudWatch alarms on failed login metrics. AWS GuardDuty also detects this pattern automatically.

---

### Rule 6 — Suspicious IP Behaviour
**Why it's dangerous:** A single user authenticating from multiple geographically distinct IPs in a short window likely indicates stolen credentials being used from a different location.

**Real-world fix:** Enforce MFA. Use IAM condition keys (`aws:SourceIp`) to restrict logins to trusted IP ranges. Enable GuardDuty — it flags impossible travel scenarios automatically.

---

### Rule 7 — Admin Overuse
**Why it's dangerous:** Frequent high-privilege actions increase the blast radius of a compromise and may indicate a threat actor performing reconnaissance or staging destructive changes.

**Real-world fix:** Require MFA for all admin actions. Use CloudTrail to alert on sensitive API calls. Break the admin role into narrower roles (e.g. `IAMAdmin`, `NetworkAdmin`, `BillingAdmin`).

---

## Sample Output

**Console (`run_phase2.py`):**
```
============================================================
  PHASE 2 — AWS JSON → Parser → Detection → Report
============================================================

[ STEP 1 ] Parsing raw AWS JSON ...

  [parser] Written 26 log rows → data/parsed_access_logs.csv
  [parser] Written 15 resource rows → data/parsed_resources.csv

[ STEP 2 ] Running detection engine ...

  [detect] 18 finding(s) returned.

[ STEP 3 ] Generating report ...

══════════════════════════════════════════════════════════════
  CLOUD SECURITY MISCONFIGURATION & ACTIVITY ANALYZER
  Generated: 2024-01-15 10:00:00 UTC
══════════════════════════════════════════════════════════════

  SUMMARY
  ──────────────────────────────────────────────────────────────
  Total findings : 18
  🔴 HIGH        : 8
  🟡 MEDIUM      : 10
  🔵 LOW         : 0
  ...
```

**Dashboard (`app.py`):** Run `streamlit run app.py` from the project root and open `http://localhost:8501`. Displays summary metric cards, a severity bar chart, a per-rule bar chart, and a filterable findings table.

---

## Limitations

- `port_open` stores a single port per resource row. A resource with multiple open ports would require multiple rows or a list field. This is a known simplification.
- Input JSON is simulated to match real AWS CloudTrail and Describe API formats — not sourced from a live AWS account.
- No machine learning or behavioral baselining. Rules are deterministic and threshold-based by design.
